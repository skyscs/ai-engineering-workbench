{
  "name": "ai-engineering-workbench",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "pnpm build:packages && pnpm --parallel --filter @aew/daemon --filter @aew/web dev",
    "build:packages": "pnpm -r --filter './packages/**' build",
    "build": "pnpm -r build",
    "typecheck": "pnpm build:packages && pnpm -r typecheck",
    "test": "pnpm build:packages && pnpm -r --if-present test && node --test scripts/tests/*.test.mjs",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "start": "pnpm --filter @aew/daemon start"
  }
}
